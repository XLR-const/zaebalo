🎤 ПОЛНОЕ ВЫСТУПЛЕНИЕ ОППОНЕНТА (Такт 2) — 8 минут
0:00 – 1:00 | ВВЕДЕНИЕ + ХОЛОДНАЯ ПОХВАЛА

    *«Коллеги, спасибо за доклад. Мы изучили ваш репозиторий — все 40+ файлов, включая код веб-приложения, обучения LightGBM, экспериментов с TimesFM, NHITS, DC-Detector и Isolation Forest. Начнём с хорошего, затем замечания, в конце — заключение.»*

    «Что работает. EDA выполнен — графики есть, артефакты по производной вычищены. Feature engineering содержит стандартный набор для временных рядов: циклическое кодирование, лаги. Это база, без которой никуда.»

    «Попробовали несколько моделей: LightGBM, CatBoost, TimesFM, NHITS. Результаты в README сведены в таблицу. LightGBM показывает наименьшую ошибку — 317 nT. На этом хорошее заканчивается.»

1:00 – 1:45 | ПРОБЛЕМА №1: SHUFFLE=TRUE

    *«Первая критическая проблема — в файле models/LightGBM/train_lightgbm.py, строки 38-40.»*

python

X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.15, shuffle=True  # ❌
)

    *«Для временных рядов shuffle=True — это data leakage. Вы перемешиваете данные, модель видит будущие точки при обучении. MAE=317 завышена. Реалистичная ошибка — 380-450 nT. Метрология невалидна.»*

    «Исправление: temporal split без shuffle, 30 минут работы.»

1:45 – 2:30 | ПРОБЛЕМА №2: HARDCODED METRICS

    *«Вторая критическая проблема — в GeoMagAnalyst/app/services/imputation.py, строки 65-70.»*

python

return {
    "filled_values": filled_values.tolist(),
    "mae": 356,                              # ❌ Заглушка!
    "model_metrics": {model_name: 10 ** 10}  # ❌ Заглушка!
}

    «Функция возвращает константы вместо реальных метрик. Это не рабочий код — это заглушка. Пользователь API видит ложные данные. Все метрики импутации в README сомнительны.»

    *«Исправление: вычислить реальный MAE/RMSE, 20 минут.»*

2:30 – 3:00 | ПРОБЛЕМА №3: НЕВОСПРОИЗВОДИМОСТЬ

    «Третья проблема: нет requirements.txt, нет pyproject.toml», нет Dockerfile». Какие версии библиотек? Результаты невозможно воспроизвести. Нарушены стандарты scientific reproducibility.»

    «Исправление: pip freeze > requirements.txt — 5 минут.»

3:00 – 4:30 | MAJOR ISSUES (БЫСТРО, ПО 30 СЕКУНД)

    «Теперь быстро по серьёзным проблемам.»

№4 — bfill в лагах:

    «В eda.py, функция _lag_features: v_temp = v.ffill().bfill(). Заполняете пропуски будущими значениями. При обучении лаги на синтетике, в продакшене будущего нет. Data leakage №2.»

№5 — Isolation Forest обещан, в коде z-score:

    «README говорит про Isolation Forest. В anomaly.py — rolling z-score. IF лежит в папке, но не интегрирован. Документация не соответствует коду.»

№6 — DC-Detector: 1 эпоха, синтетическая разметка:

    «1 эпоха обучения — этого мало. Разметка delta > 150 — синтетическая, не ground truth. Сравнение с IF некорректно.»

№7 — TimesFM: предобработка исказила сравнение:

    «Перед подачей в TimesFM вы делаете линейную интерполяцию и ffill. Модель получала уже заполненные данные. Сравнение с LightGBM некорректно.»

№8 — NHITS есть, не в продакшене:

    «NHITS обучен, есть каскадная стратегия. В финальном решении его нет. Заброшенный код в репозитории.»

№9 — FastAPI: нет Pydantic, модели грузятся при каждом запросе:

    «respons.py» пустой. В pipeline.py» модели загружаются при каждом вызове — медленно.»

№10 — нет тестов, логирования, валидации:

    «Нет ни одного теста. Нет логов. `parsing.py» не валидирует входные данные.»

4:30 – 5:30 | ПЛАН ИСПРАВЛЕНИЯ

    «Что делать. Приоритеты:»

text

Priority 1 (CRITICAL) — ~2 часа:
├── Temporal split вместо shuffle (30 мин)
├── Исправить hardcoded metrics (20 мин)
├── Добавить requirements.txt (5 мин)
└── Убрать bfill перед лагами (30 мин)

Priority 2 (MAJOR) — ~8 часов:
├── Добавить unit tests
├── Добавить логирование
├── Валидация входных данных
└── Обработка ошибок

Priority 3 (NICE) — ~10+ часов:
├── Baseline (forward fill, hourly median, ARIMA)
├── Версионирование моделей
└── Fine-tune TimesFM или убрать

    «После Priority 1 — можно использовать для экспериментов. После Priority 2 — для более широкого применения.»

5:30 – 7:00 | ВОПРОСЫ КОМАНДЕ ДОКЛАДЧИКОВ

    «Три вопроса.»

Вопрос 1:

    «Почему shuffle=True для временных рядов? Как планируете переобучить модель с temporal split? И сравните метрики — какая реальная MAE?»

Вопрос 2:

    *«В imputation.py» возвращается mae=356`. Это заглушка. Вы можете прямо сейчас вычислить реальный MAE на тестовых пропусках и сказать, совпадает ли он с 356?»*

Вопрос 3:

    «Как валидировали anomaly detection? Есть размеченные экспертом артефакты? Если нет — как оцениваете качество?»

7:00 – 8:00 | ЗАКЛЮЧЕНИЕ

    «Несмотря на проблемы, пара полезных моментов есть.»

    «Первое: честно показали, что TimesFM в zero-shot на геомагнитных данных не работает. Это экономит время другим.»

    «Второе: LightGBM с инженерными признаками даёт MAE 317 — отправная точка для следующих итераций.»

    «Третье: код читаемый, структура папок есть.»

    «Однако без исправления трёх блокеров — shuffle, hardcoded метрик, requirements — результаты нельзя признать достоверными.»

    «Рекомендуем начать с Priority 1. Спасибо.»

🔄 ВОЗМОЖНЫЕ ОТВЕТЫ КОМАНДЫ (Такт 3) И ТВОИ КОНТРАРГУМЕНТЫ (Такт 4)
Ответ команды №1: «Shuffle не влияет, у нас временные признаки есть»

Что скажут:

    «У нас есть лаги, циклическое кодирование часа — этого достаточно, shuffle не даёт leakage.»

Твой ответ (Такт 4):

    *«Не согласен. Лаги и циклические признаки не отменяют temporal order. При shuffle модель на одном примере видит будущие значения лагов из других примеров. Проведите эксперимент: обучите с temporal split и сравните MAE. Разница будет 30-60%. Это проверяемо.»*

Ответ команды №2: «Hardcoded metrics — это для теста, забыли убрать»

Что скажут:

    «Это экспериментальная заглушка, случайно попала в продакшен.»

Твой ответ:

    *«Ок, но в репозитории на момент техноразбора она есть. Пользователь API видит mae=356 всегда. Покажите коммит с исправлением — тогда сниму замечание. Сейчас это блокер.»*

Ответ команды №3: «requirements нет, но можно посмотреть в ноутбуках»

Что скажут:

    «Версии можно понять по импортам в коде.»

Твой ответ:

    *«Импорты не показывают версии. pandas 1.5 и 2.0 ведут себя по-разному. Без requirements.txt воспроизводимость нулевая. Добавьте файл — это 5 минут.»*

Ответ команды №4: «bfill — это для обучения, в продакшене его нет»

Что скажут:

    «В реальном пайплайне bfill не используется.»

Твой ответ:

    «В eda.py» функция _lag_featuresвызывается вrun_eda, которая вызывается в pipeline.py» перед импутацией. Это ваш production-код. bfill там есть. Покажите строки, где вы его убираете — я не нашёл.»

Ответ команды №5: «Isolation Forest не интегрировали, потому что z-score быстрее»

Что скажут:

    «Для продакшена скорости z-score достаточно.»

Твой ответ:

    «Ок, но тогда уберите Isolation Forest из README. Документация должна соответствовать коду. Это критерий оценки — "глубина анализа репозитория". Сейчас это введение в заблуждение.»

Ответ команды №6: «DC-Detector мы просто экспериментировали»

Что скажут:

    «Он не заявлен как финальное решение.»

Твой ответ:

    «В репозитории он лежит как равноправная модель в папке `models/anomaly_detection/models/dcdetector/». Если не используется — отметьте в README или удалите. Сейчас это мёртвый код, который отвлекает.»

Ответ команды №7: «TimesFM мы правильно сравнили»

Что скажут:

    «Предобработка была одинаковой для всех моделей.»

Твой ответ:

    «Нет. В `data_processing.py» вы перед подачей в TimesFM делаете линейную интерполяцию и ffill. LightGBM получал сырые пропуски? Покажите, что预处理 идентичен. Сейчас это искажает сравнение в пользу LightGBM.»

Ответ команды №8: «NHITS не выбрали из-за сложности»

Что скажут:

    «LightGBM проще и быстрее.»

Твой ответ:

    «Ок, но тогда покажите метрики NHITS на тех же данных. Если хуже — напишите в README и объясните почему. Сейчас NHITS просто висит непонятно зачем. Это захламление репозитория.»

Ответ команды №9: «Тесты напишем позже»

Что скажут:

    «Это исследовательский проект, тесты не приоритет.»

Твой ответ:

    *«По регламенту техноразбора мы оцениваем текущее состояние. Отсутствие тестов — минус к production readiness. 5-10 базовых тестов можно написать за час. Без них любой рефакторинг ломает код.»*

Ответ команды №10: «Вы слишком строги, это прототип»

Что скажут:

    «Мы показываем концепцию, а не продакшен.»

Твой ответ:

    «Тогда напишите в README жирным: "ЭТО ПРОТОТИП, НЕ ГОТОВ К ПРОДАКШЕНУ". Сейчас проект выглядит как готовое решение — есть FastAPI, Docker-структура, веб-интерфейс. Аудитория вводится в заблуждение.»

🎭 ЧТО СКАЗАТЬ, ЕСЛИ СПРОСЯТ ИЗ ЗАЛА (Такт 5)
Вопрос из зала: «Вы сами предложили решения? Это конструктивно.»

Твой ответ:

    «Да, по каждому замечанию дали конкретное исправление. Temporal split вместо shuffle, убрать bfill, добавить requirements, вычислить реальные метрики. В раздаточном материале есть план с оценкой времени — 2 часа на блокеры.»

Вопрос из зала: «Что самое критичное? Что убивает проект прямо сейчас?»

Твой ответ:

    «Три вещи. Первое — shuffle=True: метрики недостоверны. Второе — hardcoded metrics: код не рабочий. Третье — нет requirements: результаты невоспроизводимы. Без исправления этих трёх пунктов проекту доверять нельзя.»

Вопрос из зала: «Можно ли использовать результаты после исправления?»

Твой ответ:

    «Да. После исправления Priority 1 (2 часа) — можно для экспериментов. После Priority 2 (8 часов) — для более широкого применения. Потенциал у проекта есть.»

Вопрос из зала: «Что у проекта получается лучше всего?»

Твой ответ:

    «EDA и визуализация. Графики хорошие, очистка от артефактов обоснована. Feature engineering — стандартный, но правильный. Это база, которая работает.»

Вопрос из зала: «Агентность в проекте есть? По регламенту спрашиваю.»

Твой ответ:

    «Нет. Проект не про агентов. Это классический ML-пайплайн для временных рядов. Агентность не заявлена, поэтому вопросов нет. Но если бы заявили — было бы несоответствие.»

Вопрос из зала: «Вы бы взяли этот код в продакшен?»

Твой ответ:

    «В текущем виде — нет. Из-за трёх блокеров. После исправления — возможно, как прототип. Но нужны ещё тесты и валидация.»

ИТОГОВАЯ ШПАРГАЛКА (ДЛЯ СЕБЯ, РАСПЕЧАТАТЬ)
text
```
┌─────────────────────────────────────────────────────────────────┐
│  ТАКТ 2 (ВЫСТУПЛЕНИЕ) — 8 мин                                    │
├─────────────────────────────────────────────────────────────────┤
│  0:00-1:00  Холодная похвала (EDA, FE, модели)                  │
│  1:00-1:45  Problem #1: shuffle=True → data leakage             │
│  1:45-2:30  Problem #2: hardcoded metrics → заглушка            │
│  2:30-3:00  Problem #3: нет requirements → невоспроизводимо     │
│  3:00-4:30  Major issues (bfill, IF vs z-score, DC, TimesFM,    │
│             NHITS, FastAPI, тесты) — по 30 сек                   │
│  4:30-5:30  План исправления (Priority 1/2/3)                   │
│  5:30-7:00  Вопросы команде (3 вопроса)                         │
│  7:00-8:00  Заключение (формально-позитивное)                   │
├─────────────────────────────────────────────────────────────────┤
│  ТАКТ 4 (ОТВЕТНОЕ СЛОВО) — 3 мин                                 │
├─────────────────────────────────────────────────────────────────┤
│  Их ответ про shuffle → "Проведите эксперимент, разница 30-60%" │
│  Их ответ про hardcoded → "Покажите коммит с исправлением"      │
│  Их ответ про requirements → "Импорты не показывают версии"     │
│  Их ответ про bfill → "В production-коде bfill есть"            │
│  Их ответ про IF vs z-score → "Уберите из README"               │
│  Их ответ про прототип → "Напишите в README, что это прототип"  │
├─────────────────────────────────────────────────────────────────┤
│  КЛЮЧЕВЫЕ ФРАЗЫ                                                  │
├─────────────────────────────────────────────────────────────────┤
│  "Метрология невалидна"                                          │
│  "Это заглушка, не рабочий код"                                  │
│  "Результаты невоспроизводимы"                                   │
│  "Документация не соответствует коду"                            │
│  "Без исправления трёх блокеров доверять нельзя"                 │
└─────────────────────────────────────────────────────────────────┘
```
